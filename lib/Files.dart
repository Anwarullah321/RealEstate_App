import 'package:flutter/material.dart';

class Files_Page extends StatelessWidget {
  const Files_Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
      child: Text('files page'),
      ),
    );
  }
}
